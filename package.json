{
  "name": "kashan-ahmad.github.io",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "preversion": "git fetch --tags",
    "postversion": "git push --follow-tags",
    "commit": "git add . && git commit -m \"build: release prepared\"",
    "push": "git push --follow-tags",
    "release:prepare": "npx changeset",
    "release": "npx changeset version",
    "clean": "rm -rf dist && rm -rf node_modules && pnpm i"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^1.0.0",
    "@astrojs/tailwind": "^1.0.0",
    "astro": "^1.0.0",
    "astro-robots-txt": "^0.3.8"
  }
}
