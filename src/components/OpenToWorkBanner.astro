---
import { Icon } from "astro-icon";
import { routes } from "@utils/routes";
import { strings } from "@utils/strings";
---

{
  strings.isOpenToWork && (
    <div class="bg-black p-8 md:px-20 md:py-20 mt-20 mx-auto rounded-lg flex flex-col items-center text-center">
      <span class="text-white">
        <Icon name="bx:bxs-bowl-rice" class="w-20 h-20 mb-8" />
      </span>
      <h2 class="text-white text-3xl sm:text-4xl md:text-6xl tracking-tight">
        I'm{" "}
        <span class="underline underline-offset-4 decoration-pink-600">
          {strings.openToWork}
        </span>
      </h2>
      <p class="text-slate-400 mt-4 text-lg md:text-xl">
        Im always looking for new problems to solve and new people to work with.
        If you have a project, think you need my help, or just fancy a chat,{" "}
        <a
          href={routes.contact}
          class="before-ornament text-slate-200 hover:text-pink-400 focus:text-pink-400">
          get in touch
        </a>{" "}
        👋
      </p>
    </div>
  )
}
